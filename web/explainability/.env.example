# API Configuration
# Base URL for the CryptoFunk API server
# Leave empty to use relative URLs (when using Vite proxy in development)
# Set to full URL in production (e.g., https://api.cryptofunk.example.com)
VITE_API_BASE_URL=

# Example configurations:
# Development with proxy (default): VITE_API_BASE_URL=
# Development direct: VITE_API_BASE_URL=http://localhost:8080
# Production: VITE_API_BASE_URL=https://api.cryptofunk.example.com
