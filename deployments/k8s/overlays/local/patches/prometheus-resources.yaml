apiVersion: apps/v1
kind: Deployment
metadata:
  name: prometheus
  namespace: cryptofunk
spec:
  template:
    spec:
      containers:
      - name: prometheus
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
