apiVersion: apps/v1
kind: Deployment
metadata:
  name: orchestrator
  namespace: cryptofunk
  labels:
    app.kubernetes.io/name: orchestrator
    app.kubernetes.io/component: mcp-coordinator
spec:
  replicas: 1  # Single orchestrator coordinates all agents
  selector:
    matchLabels:
      app.kubernetes.io/name: orchestrator
  template:
    metadata:
      labels:
        app.kubernetes.io/name: orchestrator
        app.kubernetes.io/component: mcp-coordinator
    spec:
      # Use Vault service account for Kubernetes authentication to Vault
      serviceAccountName: cryptofunk-vault

      containers:
      - name: orchestrator
        image: cryptofunk/orchestrator:v0.10.0
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8081
          name: http
          protocol: TCP
        env:
        # Vault Configuration (when VAULT_ENABLED=true, secrets come from Vault)
        - name: VAULT_ENABLED
          valueFrom:
            configMapKeyRef:
              name: vault-config
              key: VAULT_ENABLED
        - name: VAULT_ADDR
          valueFrom:
            configMapKeyRef:
              name: vault-config
              key: VAULT_ADDR
        - name: VAULT_AUTH_METHOD
          valueFrom:
            configMapKeyRef:
              name: vault-config
              key: VAULT_AUTH_METHOD
        - name: VAULT_MOUNT_PATH
          valueFrom:
            configMapKeyRef:
              name: vault-config
              key: VAULT_MOUNT_PATH
        - name: VAULT_SECRET_PATH
          valueFrom:
            configMapKeyRef:
              name: vault-config
              key: VAULT_SECRET_PATH
        - name: VAULT_K8S_ROLE
          valueFrom:
            configMapKeyRef:
              name: vault-config
              key: VAULT_K8S_ROLE
        - name: VAULT_NAMESPACE
          valueFrom:
            configMapKeyRef:
              name: vault-config
              key: VAULT_NAMESPACE
              optional: true
        - name: DATABASE_URL
          value: "postgresql://$(POSTGRES_USER):$(POSTGRES_PASSWORD)@$(POSTGRES_HOST):$(POSTGRES_PORT)/$(POSTGRES_DB)?sslmode=disable"
        - name: POSTGRES_USER
          valueFrom:
            configMapKeyRef:
              name: cryptofunk-config
              key: POSTGRES_USER
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: cryptofunk-secrets
              key: POSTGRES_PASSWORD
        - name: POSTGRES_HOST
          valueFrom:
            configMapKeyRef:
              name: cryptofunk-config
              key: POSTGRES_HOST
        - name: POSTGRES_PORT
          valueFrom:
            configMapKeyRef:
              name: cryptofunk-config
              key: POSTGRES_PORT
        - name: POSTGRES_DB
          valueFrom:
            configMapKeyRef:
              name: cryptofunk-config
              key: POSTGRES_DB
        - name: REDIS_URL
          value: "redis://$(REDIS_HOST):$(REDIS_PORT)/$(REDIS_DB)"
        - name: REDIS_HOST
          valueFrom:
            configMapKeyRef:
              name: cryptofunk-config
              key: REDIS_HOST
        - name: REDIS_PORT
          valueFrom:
            configMapKeyRef:
              name: cryptofunk-config
              key: REDIS_PORT
        - name: REDIS_DB
          valueFrom:
            configMapKeyRef:
              name: cryptofunk-config
              key: REDIS_DB
        - name: NATS_URL
          value: "nats://$(NATS_HOST):$(NATS_PORT)"
        - name: NATS_HOST
          valueFrom:
            configMapKeyRef:
              name: cryptofunk-config
              key: NATS_HOST
        - name: NATS_PORT
          valueFrom:
            configMapKeyRef:
              name: cryptofunk-config
              key: NATS_PORT
        - name: BIFROST_URL
          value: "http://$(BIFROST_HOST):$(BIFROST_PORT)"
        - name: BIFROST_HOST
          valueFrom:
            configMapKeyRef:
              name: cryptofunk-config
              key: BIFROST_HOST
        - name: BIFROST_PORT
          valueFrom:
            configMapKeyRef:
              name: cryptofunk-config
              key: BIFROST_PORT
        - name: LOG_LEVEL
          valueFrom:
            configMapKeyRef:
              name: cryptofunk-config
              key: LOG_LEVEL
        - name: CRYPTOFUNK_ORCHESTRATOR_METRICS_PORT
          value: "8081"
        resources:
          requests:
            cpu: "200m"
            memory: "512Mi"
          limits:
            cpu: "1000m"
            memory: "2Gi"
        livenessProbe:
          httpGet:
            path: /liveness
            port: 8081
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /readiness
            port: 8081
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
